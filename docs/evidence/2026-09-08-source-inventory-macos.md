# Read-only source inventory on macOS

## Scope and environment

Validated the `inspect-sources` CLI and opt-in source panel built on base `8bb3851`. This is real-source reading plus owned-fixture regression evidence, not personal-source registration, removal or desktop mode verification.

- Native macOS 26.6.2 arm64, Node.js 24.20.0, native Codex CLI 0.153.4. The selected CLI executable belongs to the ChatGPT 26.901.51231 bundle, build 8109; this identifies the executable's container rather than an attached desktop runtime.
- Existing loopback GUI/store/scope reopened for the whole check. No new demo replaced the retained data.
- Built React/TypeScript/Vite/PixiJS interface tested in the Codex in-app browser at a normal 1237px width and 390 × 844. No separate browser fallback was used.
- Private raw reports, selected-file hashes and test logs remain in ignored local evidence. No personal source text, paths, Skill names, hook commands or recordings are published here.

## Observed inventory

The real native CLI completed the recognized RPC queries and bounded standard-file census with both absolute and relative selected cwd input. It reported two configuration layers, 60 discovered Skills (33 enabled, 27 disabled), and one hook. The recognized memory/plugin/MCP keys were present in one reported configuration layer. These are discovery/presence results, not proof of active loading or user authorship.

Three standard instruction candidates contained text; one other standard candidate was absent. Both global override/base candidates remain listed. No claim is made about which candidate Codex actually loaded, custom fallback names or host-injected sources. All personal-source registration, control and full runtime/mode flags remain unverified/false.

The selected global configuration and two global instruction files had unchanged content/existence before and after the work. The selected project configuration was also unchanged. The repository's own `AGENTS.md` changed deliberately to record the maintainer's accepted product scope; it is not included in the unchanged-file claim.

## Validation

- `node --test`: **176 passed, 0 failed, 0 skipped**.
- `npm ci --ignore-scripts`, TypeScript/CSP checks and production build completed.
- Synthetic coverage includes the request allowlist and private-value projection, source census bounds and unsupported file types, relative cwd normalization, selected directory replacement, opt-in scope, browser selector rejection, duplicate/concurrent reads, and fixture saving during delayed collection.
- An independent code review found a reconnect bug: authentication could advance while the UI still held an old target. The fix compares a non-authenticating launch identity with metadata accepted by the UI. A regression reproduces metadata transport failure after reconnect and verifies that the next attempt still stops for target display before any collection. Final review found no remaining blocking issue.

## Browser flow

| Check | Observed result |
| --- | --- |
| Page identity and first render | Expected local page, meaningful controls and existing artwork; no framework error overlay |
| Explicit read | The selected real project produced the inventory above; no personal setting was applied or saved |
| Page reload | The dated server report was displayed without another automatic collection |
| Explicit reread | A new collection updated the timestamp |
| Same URL, different startup cwd | The old read button displayed the new target and left it uncollected |
| Same URL, inventory disabled | The old target disappeared; a metadata-refresh action remained available without an error loop |
| Re-enabled startup | Metadata first displayed the selected project; the following explicit read collected it |
| Simple source hierarchy | Three source categories shown by default; retained memory/integration/policy details opened and closed independently |
| Narrow viewport | Controls and details remained readable; document width matched the 390px viewport, including expanded target information |
| Browser health | No relevant warning/error logs in the final check |

Native screenshots were reviewed inline during the interactions. No saved screenshot artifact is claimed. The accepted renderer, scene, pose data and artwork files were unchanged by this feature. A final preview selection used a read-only plan; the fixture remained baseline revision 13, with no application, no conflict and full runtime/mode flags false.

## Product boundary

The maintainer selected self-authored/personally added optional instructions and Skills as the main release candidates, with optional hooks secondary. Existing memory and native task-continuity settings stay in the shared environment. The [scope note](../harness-scope.md) separates that adopted policy from the proposed minimal-guide/blank-extra comparison.

Individual target customization, personal setting import/switch/recovery, and a finalized minimal replacement guide are not implemented in this slice. No model comparison or performance improvement was measured. This pass adds no Windows or Claude Code evidence and does not resolve the existing Windows baseline-record mismatch.

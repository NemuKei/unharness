# Pre-use starting conditions on macOS

Native and browser checks ran on 2026-09-08–09 JST. The implementation fixes the request, task-defined criteria/budget and original working files before an explicitly requested later replay. It starts no model task and does not establish a performance verdict or full Mac Codex support.

## Tested implementation

- Input/declaration core: `c2b14de`.
- Registered capture service: `96860d4`.
- Final CLI/GUI implementation: `6dc056fbc8b7b954f108bede050e36b328061f6f`.
- macOS 26.6.2, arm64; Node 24.20.0; installed native Codex 0.153.4; Chrome 152.0.7977.82.
- Browser plugin unavailable in this session; used the existing bundled Playwright library with installed Chrome, adding no application dependency.
- Isolated, newly created source profiles and synthetic requests/files only. No personal configuration or existing user's task was changed, captured or started for this check.

## Native file/service evidence

The native Codex discovery/registration route created a separate owned Normal. The synthetic Git project held seven present files and one missing tracked path, totalling 1,048,767 input bytes. It included an uncommitted edit, an untracked file, a 1,048,583-byte binary, an ignored project configuration and an explicitly selected ignored input.

Review → save → change the original working file → reread retained the original request, declaration and exact input bytes. Re-saving the same review returned its existing saved-start identity despite the later working-file edit. A new file introduced after another review prevented that review from being saved. Git metadata was excluded. Selected source bytes/metadata and private source-state JSON remained equal; the profile stayed prepared Normal with no conflict or pending recovery. Model tasks started: **0**.

Input chunks/manifests use a separate `input` bucket and saved-start indexes use `experiment`. Missing optional buckets in older stores read as empty without mutation and are created only during explicit publication. Corrupt/missing chunks, malformed manifests, unsafe paths, links, oversized data and a project containing its own private store are refused. Ordinary source/comparison reads do not enumerate input blobs and remain usable if an optional input is corrupt.

## Browser evidence

The native-profile workbench's served index matched the built index. At 1440×1000 and 390×844, the flow exercised the collapsed entry, exact request, requirement/anchor fields, explicit budget, added ignored input, eight-entry file review, save, explicit detail and reload. Input fields cleared on reload while saved starts remained readable. Effects were off, no horizontal document overflow appeared, and the narrow file table had equal 326-pixel content and viewport widths after layout refinement. The saved request rendered as plain text.

Production-browser regressions also verify:

| Case | Observed result |
| --- | --- |
| Edit after review | Old save action disappears; a new review is required |
| Delayed review after an edit | Old result and progress indication are discarded |
| Edit while a save response is held | Confirmed save remains recorded without replacing the newer draft |
| Successful save, failed history refresh | Saved result stays confirmed; list failure is separate |
| Delivered save, dropped response | Result stays uncertain; no automatic retry |
| Failed explicit history read after an uncertain save | Uncertainty remains available for a later successful history confirmation |
| Same-port server relaunch | Draft, review, detail and cached history clear |
| Same URL rebound to another home/project | Old reviewed save stops before a write to the new scope |
| Existing comparison UUID handoff | Repeated explicit handoffs still work |

No uncaught page error or framework overlay was observed. Chrome logged the existing `/favicon.ico` 404, corroborated through the console's resource URL. That asset remains a Mac product-finish item. Deliberately injected transport failures are separate from the normal console check.

After integration, the existing real workbench was restarted on its original loopback port. Its full returned source state and the bytes/metadata of all six registered managed file entries matched the before-upgrade capture. It remained Normal revision 4 with the existing source conflict and no pending recovery; the launch identity changed and the served index matched the main build. No retained-settings plan was accepted and no personal starting condition was created.

## Validation and limits

The final complete suite at `6dc056f` passed **486 tests**, with no failures and one existing platform skip (**487 total**). All **16** targeted HTTP/controller/browser tests passed, as did TypeScript, Pixi CSP and the production build. The final narrow layout was read from the rebuilt server and visually checked again. Private command logs and screenshots are retained outside tracked source; no file/request bodies or private workspace paths are included here.

These records are immutable input preparation. They do not copy a task into another work location, submit a request to Codex, associate a desktop recording with the request, establish identical live memory/tools, compare outcomes or unlock an original form. Continue sequential replay and AI/MCP under [delivery](../delivery.md); Claude Code and Windows qualification remain separate.

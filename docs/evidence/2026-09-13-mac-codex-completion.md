# Mac Codex 0.0.6 qualification

Date: 2026-09-13 (JST). This completes the installed-package checks for the
maintainer-approved Mac scope: optional additional instructions and ordinary
Skill disabled/manual/automatic states, with official plugins retained at
Normal. Individual remote-plugin OFF remains
[deferred](2026-09-11-remote-plugin-control-limit.md).

## Distribution

| Item | Verified value |
| --- | --- |
| Platform | Apple Silicon, macOS 26.6.2 |
| Desktop / native executable | Codex Desktop 26.903.71938 / Codex 0.153.4 |
| Bundled Node | 24.20.0, darwin-arm64 |
| Package | 0.0.6 |
| Clean implementation source | b384bddc39805b0bccffce71ab981e7c133a51bb |
| Distribution ID | b34064ce6a91d48309e92481e3869e9d6af92b1ea986571ddfb6fee128a0a9f2 |
| Indexed files | 7,441, plus the distribution index |
| ZIP bytes | 93,584,694 |
| ZIP SHA-256 | 8e91968f875f1d43a33e48783151f7370c8a07579e408214f7daf729e39d8e69 |

The [public release](https://github.com/NemuKei/unharness/releases/tag/v0.0.6),
its source tag and both assets were read without authentication. The downloaded
ZIP, published asset digest and checksum sidecar agree. Mac ditto extraction
preserves every indexed file and executable bit. The bundled Node signature
verifies. Published 0.0.1 and 0.0.4 archives were not replaced; the internal
0.0.5 candidate was not published.

The [reconciliation fix](2026-09-13-retained-separator-merge.md) handles native
reinstallation adding a separator or moving a retained table beside a saved
Skill block. The full source regression passes **1,253 of 1,324 cases**, with
71 explicit platform/optional-browser skips and no failures. Type/CSP and
both builds pass. The final change retains the complete typed proof of the
selected and retained values.

## Installed package and public operations

The previously qualified owned profile was updated through the real native
marketplace/plugin commands. Every step preserved unrelated configuration.
The installed file index matched the distribution above, its native MCP
exposed 65 tools, and an independent recovery copy was prepared before update.
The retained-only installation change was reviewed and recorded without
changing managed sources.

The installed 0.0.6 MCP opened its local workbench and requested the public
connection. The actual Codex in-app browser displayed the exact origin,
synthetic registered scope and thirteen operations before local approval.
The one-time fragment was removed before the HTTPS page connected to loopback.
The page exposed its real eleven WebMCP tools.

| Entry | Result |
| --- | --- |
| WebMCP plan/apply | The original saved TRUEFORM prepared successfully |
| Public GUI plan/apply | The original saved UNSEAL prepared successfully |
| WebMCP receipt read | The GUI operation's exact successful receipt was returned |
| WebMCP plan/apply | Current Normal was restored |
| Independent readback | All registered file bytes and metadata matched current Normal; the original setup stayed unchanged |
| Local revocation | The page removed current state and returned expired with state null |
| Native MCP after revocation | All three successful public operation receipts remained readable |

These are prepared-file and transport results. They do not claim that a
running model task instantly changed mode.

## Independent recovery

A process-specific macOS sandbox denied external networking, native Codex
execution and reads of the original plugin cache. Separate probes returned
EPERM for all three and confirmed that loopback HTTP still worked.

The independent 0.0.6 copy opened its recovery screen on the first launch
under that restriction. The actual in-app browser restored the owned profile
from TRUEFORM to Normal. Readback under the same restriction matched all
Normal bytes and metadata, preserved the original setup and favorite, and
found no pending journal.

This tests those process restrictions; it does not claim that host Wi-Fi or
the entire desktop app was shut down. Actual native plugin removal and
cache-absence recovery remain separately recorded in the
[0.0.4 qualification](2026-09-11-mac-codex-0.0.4.md).

## Personal installation and previous evidence

The personal profile was also updated to the same indexed 0.0.6 package.
Only its Unharness marketplace coordinates changed; unrelated parsed values,
nonblank lines and current metadata were preserved. The existing binding,
paired setup and 453 prior immutable records remained intact. Independent
retained settings were reviewed and recorded as current Normal without
rewriting any current source file.

A new Desktop task used the installed management Skill, verified the 0.0.6
distribution and opened the registered personal workbench. Its first read
correctly reported a conflict: creating that verification task had added
exactly one trusted project location in Codex settings. That existing setting
was preserved through a retained-only recording. The personal profile then
read Normal revision 35 with no conflict or pending recovery. The same model
task independently re-read that state and confirmed the live management
screen and all 7,441 indexed package files.

The packaged distribution index is the release-version evidence. The MCP
server's observed self-reported version remains 0.0.1 and is not used to
identify the package.

The unchanged onboarding, three fresh model input observations, model-led
paired setup, ordinary comparisons, legacy favorites, original artwork,
collection reselection and downloaded image card are documented in the
[0.0.4 native journey](2026-09-11-mac-codex-0.0.4.md). Its model-input evidence
is dated to that package and desktop build; the extra 0.0.6 task checks the
current management entrance. Whole-plugin runtime and performance improvement
remain unqualified. Claude Code, Windows and Intel Mac remain outside this
release's qualified scope.

## Public introduction

The public introduction displays the requested original AI-persona example
in three poses. Final site source 674e8942684cdc56a84b89535dd4830ccbccc9f2
was deployed to the existing dedicated Pages project as
cef3e95d-cfe6-402b-aa23-9f799b6640f2. All 22 served files match the build,
including the self-only script policy and the exact 0.0.6 release coordinates.

The actual in-app browser displayed 0.0.6 and the expected archive URL, SHA-256,
distribution ID and source revision in its installation request. Its copy
action reported success. Three final built-browser installation/entry cases
passed without skips. The final page connected to the installed 0.0.6 runtime
and WebMCP read the owned profile's Normal state without conflict or pending
recovery. Local revocation returned expired with state null. The owned
verification server was then stopped without removing saved data.

The public example rendered at the narrow in-app viewport, with the three
poses in its horizontal gallery. The final page reported no warning or error
logs. Together with the dated model, setup and artwork evidence above, these
checks satisfy the agreed first Mac Codex release scope.

Raw configurations, private paths, authentication, connection tickets,
personal task recordings and artwork data remain outside this report and Git.

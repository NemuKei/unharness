# Public site and current local GUI — 2026-09-21

The maintainer authorized deployment and selected the public site plus the
current local GUI. A new Mac archive and installed-plugin update were excluded
from this publication scope.

## Published site

Clean source `aedb7b912504e4ecc82e83f77a1cfb9aa008b775` was pushed to `main`
and deployed to the existing `deltahelmlab-unharness` Cloudflare Pages project.
Production deployment `ffbef08b-ec65-4c24-9f10-08d5c5104cd8` succeeded at
2026-09-21 04:13:06 UTC. Its custom domain remains
[unharness.deltahelmlab.com](https://unharness.deltahelmlab.com/).

Anonymous HTTPS reads verified all 49 served files, totaling 10,374,738 bytes,
against the local site build. Every file returned HTTP 200 and matched exactly.
The document retains CSP, no-referrer, nosniff, DENY and no-cache headers; the
release catalog retains no-store. The catalog and downloadable Mac package
remain 0.0.10, with no archive replacement or marketplace update.

The actual HTTPS site in the Codex in-app browser displayed the updated
introduction and the separate local-opening guide. Japanese/English navigation,
the Codex draft label and 390px layout passed; the document did not overflow and
no console warnings/errors were recorded. The native draft link was inspected,
not submitted. No new public pairing or personal mode operation was performed.

## Current local development GUI

The existing authenticated owned launcher was opened from the same registered
workspace after rebuilding the local bundle. It replaced only its verified
owned GUI process. The existing in-app tab was navigated to the newly returned
loopback URL and displayed the refreshed mode cards, saved-content view and
theme controls.

Before and after opening, registered file contents/metadata, workspace state and
registration were compared by digest and matched. The stored TRUEFORM preparation
and revision 41 remained unchanged. The pre-existing source-conflict state was
retained and displayed as requiring review, not mislabeled as verified current
runtime state. No mode, setup, Normal, Skill or artwork was applied.

The served local HTML matched the new build; the registered profile/project
identity matched the prior GUI. Its new view loaded without console warnings or
errors. This verifies the currently running development GUI, not installation of
a new plugin version or fresh-task loading of the host's MCP/Skills.

## Validation and boundaries

The preceding integrated source passed 1,330 Node tests with 88 explicit skips
and zero failures. Browser regression ran separately: 87 cases passed with at
most two test files concurrently, then the final operation-ID correction passed
all four public workbench cases. Public artwork/lifecycle groups passed 5/5 and
3/3. Pro's integration finding was reproduced, fixed and rechecked. Type/CSP,
local/site builds and staged whitespace checks passed before publication.

This release action adds no Windows/Claude qualification, native screen-reader
or native 200% zoom evidence. The published website, current development GUI,
0.0.10 archive and installed plugin remain separately identified surfaces.

# Additive Skill enrollment on macOS

Observed on 2026-09-09, macOS 26.6.2 / Darwin 25.6.0, arm64. The installed desktop application's Codex executable reported **0.153.4**. These checks qualify the additive registration/preparation boundary on owned profiles. They do not establish fresh desktop task loading, personal-source enrollment, the installer or a completed Mac release.

## Preserved boundaries

- A current fixed-context inventory and confirmed source roles produce an immutable enrollment review. Unknown roles, invented paths, unavailable sources, stale inventories and stale source revisions are refused.
- Adoption expands the registration, saved Normal and release presets without changing source files. The original reservation/manifest, earlier managed Normal content, favorites, starts and comparison/replay records remain unchanged.
- The new scope requires a mode preparation. Until then the UI labels the previous preparation, current observation and favorite capture are unavailable, and ordinary measurements have no current-source association.
- Historical favorites retain their earlier behavior. Newly enrolled Skills are included in their saved Normal state through an explicit restoration preview; previously disabled Skills stay disabled.
- Both enrollment interruption phases cancel through the Node-only recovery route. Independent source edits and foreign state stages are preserved. Active replay or appearance recovery prevents enrollment.
- The actual previous writer from `e26a49f18e5acf3e918c3169d53dcd987dd58163` successfully plans Normal before enrollment and refuses the expanded target set afterward. It makes no source or state change on rejection.

## Checks

| Check | Observed result |
| --- | --- |
| Complete working-tree Node suite | 750 passed, 0 failed, 26 platform/optional-browser skips out of 776 tests |
| Focused enrollment/setup/observation/history/MCP/update suite after the preparation guard | 161 passed, 0 failed |
| Final historical-native-reference refinement | 20 source-enrollment/replay-result tests passed |
| Isolated staged sources, excluding unfinished appearance integration | 28 core/MCP/update tests passed; TypeScript, CSP guard and Vite build passed |
| Built enrollment GUI on desktop and 390 px width | Two browser cases passed, also against the isolated staged build; no unexpected console/page errors or horizontal overflow |
| Existing setup handoffs | Three browser cases passed |
| Installed native Codex executable | Owned profile expanded from one to two Skills; TRUEFORM, UNSEAL, Normal and the original favorite prepared successfully |

The native run used a newly created private profile and no model task. Enrollment changed zero configuration files. TRUEFORM produced the explicit-invocation policy for the new Skill; UNSEAL retained its reviewed automatic choice; Normal and the old favorite returned the original managed files and supported metadata exactly. The added Skill body remained unchanged. Final state had no conflict or pending recovery, and the private test profile was removed. Personal configuration was not enrolled or modified.

The browser cases exercised unconfirmed origin, a concrete two-step review/adopt flow, state refresh after the scope change, the required subsequent mode preparation, and restoration of an older favorite with the added-Skill notice. A simulated lost adoption response produced one POST only; explicit readback revealed the saved scope without resending the mutation. Screenshots were inspected locally and are not stored in Git.

MCP checks used the official client. Duplicate enrollment calls returned the same receipt across the scope change and reconnect. Later writes using an obsolete accepted context were refused until `status` accepted the successor. CLI and HTTP rejected duplicate JSON keys, arbitrary workspace/context fields and oversized input; HTTP authentication/origin checks remained intact.

See the [enrollment contract](../spec-guided-setup.md#additive-skill-enrollment), [registered-source runbook](../user-source-gui.md), and [AI operations](../ai-commands.md). Native desktop loading of the new setup/enrollment journey remains part of the Mac Codex product finish.

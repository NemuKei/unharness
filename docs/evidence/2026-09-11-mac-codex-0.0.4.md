# Mac Codex 0.0.4 qualification

Date: 2026-09-11. This qualifies the maintainer-approved Mac scope:
confirmed optional additional instructions and ordinary Skill states, with
official plugins retained. Individual remote-plugin OFF remains excluded
under the [observed control limit](2026-09-11-remote-plugin-control-limit.md).

## Immutable distribution

| Item | Value |
| --- | --- |
| Target | Apple Silicon macOS 26.6.2 |
| Desktop / native executable | Codex Desktop 26.903.61454 / Codex 0.153.4 |
| Bundled Node | 24.20.0, darwin-arm64 |
| Implementation source | `f785edd9fc5a7061723133a34eddd3aa4998ef22`, clean at assembly |
| Package version | 0.0.4 |
| Distribution ID | `9d4c0cb53618fcb08fa9cad1d51afa65be5469bf1b8f9be43da5722ef895ace6` |
| Indexed files | 7,439, plus the distribution index |
| Installation ZIP | 93,578,580 bytes |
| ZIP SHA-256 | `ef2e44aee408f20fce05c1ae76cdade3f9879485c09775abee1e16b2dc752221` |

The complete index and bundled Node signature matched before native
installation and after macOS ditto ZIP extraction. The
[published archive](https://github.com/NemuKei/unharness/releases/tag/v0.0.4),
its checksum file and Git tag were retrieved and checked; an unauthenticated
ZIP download matched the exact bytes above. Version 0.0.1 was not replaced.

The full Node suite passed 1,247 of 1,318 cases, with 71 explicit platform or
optional-browser skips and no failures. All eight separately enabled built
browser cases passed. A further 20 built public-client, installation,
connection and artwork cases passed with no skips after pinning the published
archive. Focused control checks passed 18 cases; the
state/enrollment/MCP group passed 36. Type/CSP checks, both production builds,
vendored TOML/merge integrity and changed documentation links passed.

## Installation and native inputs

Two distinct paths were exercised rather than presenting them as one record:

- A fresh, marked synthetic profile installed the extracted ZIP through the
  actual native marketplace/plugin commands. No authentication file was
  copied. Its installed MCP exposed 65 tools and first reported an
  unconfigured connection. The bundled launcher configured it with only
  system directories in PATH and prepared an independent recovery copy.
  The actual in-app browser selected only the newly authored fixture guide
  and Skill and saved Normal without changing their bytes or metadata.
- The existing personal profile was updated to the same immutable package.
  Every native update step preserved unrelated parsed configuration. The
  existing binding and saved history remained in place; the reviewed
  installation-related retained change became a new Normal version.

In the fresh profile, actual native Skill readback distinguished disabled
(`enabled=false`), manual (`enabled=true`, implicit invocation false), and
automatic (`enabled=true`, implicit invocation true). The three-mode cycle
returned all source bytes/absence and metadata to Normal. No model task was
submitted under that synthetic profile.

Three actual Desktop model tasks in the personal profile then corroborated
TRUEFORM, UNSEAL and restored Normal. The recorded selected-source inputs
matched: inert instructions and an absent automatic Skill entry in TRUEFORM;
the minimal guide and automatic entry in UNSEAL; saved instructions and the
automatic entry in Normal. All recorded model/reasoning, execution-policy
and required-project instruction digests agreed, and memory guidance remained
recorded. Explicitly naming the manual Skill read its actual current file;
the Skill's external operation was not executed.

The overall observation remains unknown for whole-plugin runtime. Its Skill
inputs matched the retained dependency, but no MCP/hook/app/scheduled-task
runtime evidence is invented. The three ordinary run records can be viewed
together; their assessment remains neutral and does not establish performance
improvement.

## AI, favorites and artwork

In a fresh TRUEFORM task, the model read the installed setup Skill and saved
the approved pair through the installed MCP: manual ordinary Skill in
TRUEFORM, automatic in UNSEAL with the minimal guide, and registered plugins
at Normal in both. An independent read confirmed the saved definition,
unchanged Normal and unchanged prepared files.

The task did not expose Unharness tools directly in its tool list. The model
used the bundled launcher to connect to the same bound MCP over stdio, without
installing Node/npm or directly rewriting records. This proves that local MCP
route; it does not claim automatic tool-list exposure in every Desktop task.

The actual in-app browser restored an original legacy Normal favorite while
retaining current shared settings and the later plugin's Normal value.
All 396 pre-existing immutable records stayed unchanged. New versioned
TRUEFORM and UNSEAL favorites were also saved.

A separate fresh Normal task used the original-artwork Skill and owned
authoring place to import the supplied 724px entity PNG. The standard
background/restraint references were retained. The stored content matched the
public example after canonicalizing asset-array order; array order was the
only difference in the first overly strict comparison.

The imported version was re-read, selected, switched to the prepared appearance
and selected again. The collection, exact asset IDs and Normal preparation
remained intact. The model rendered and downloaded a 1,200 by 820 card with all
three poses, and the parent independently viewed that file. A local caption
was prepared; nothing was posted externally. Artwork creation stayed available
with unknown comparison evidence.

## Independent recovery

The personal recovery copy started on its first attempt with a process-only
sandbox that denied external networking, native Codex execution and reads of
the original plugin cache. Controls separately confirmed EPERM for all three
prohibited operations and successful local HTTP. The operating system's
network settings and other applications were unchanged.

The actual in-app recovery screen restored TRUEFORM to Normal. Readback under
the same restriction, using only the independent package's Node and modules,
confirmed exact Normal files, preserved setup and no pending journal.

The fresh synthetic profile additionally removed its actual native plugin
cache and registration. The personal installation remained unchanged.
Its independent copy opened with external networking denied, reviewed the
removal as a retained-only change through the local Codex executable, and
restored Normal in the browser. Independent readback confirmed the retained
removal, favorite/setup preservation and the still-absent original cache.

These checks qualify the stated process restrictions and removal paths.
They do not claim that Wi-Fi, the entire desktop app or the host OS was shut
down, nor add Windows, Intel Mac or Claude Code qualification.

## Public-site follow-through

The archive is public and the site metadata now selects its verified version,
checksum, content identity and source revision. The static production rollout
and native HTTPS/WebMCP readback are the remaining publication checks.

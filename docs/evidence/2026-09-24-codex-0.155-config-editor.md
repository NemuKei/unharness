# Codex 0.155.0-alpha.16.3 の設定編集確認（2026-09-24）

- 実行ファイル：`/Applications/ChatGPT.app/Contents/Resources/codex`（SHA-256: `c67698d0990aae05211d9c43ab343ad9517e406824dea77eca103a2806232b3a`）。`initialize` の `userAgent` は `0.155.0-alpha.16.3` を返した。
- `HOME` と `CODEX_HOME` を一時的な合成ディレクトリに向け、合成 Skill と `config.toml` だけを使用した。一時ディレクトリは確認後に削除した。利用者の Codex 設定は読み書きしていない。
- 最初の user layer には合成 Skill の `enabled = true` があった。`skills/config/write` で `false` にすると、`config/read` は `false` を返した。コメント、`model`、`approval_policy` は保持された。
- 同じ API で `true` に戻すと応答は `effectiveEnabled: true` だったが、`config/read` の user layer と保存された TOML から Skill の明示的な項目が消えた。元のバイト列には戻らなかった。
- 比較対象の既存 `test/fixtures/config-editor-server.mjs`（0.153.4 の合成契約）では、有効に戻した後も `enabled = true` の明示的な項目が残り、元のバイト列に戻る。**復元結果が一致しないため、0.155.0-alpha.16.3 は確認済み版に追加しない。** 実物の 0.153.4 実行ファイルとの直接比較は行っていない。
- 未確認：`config/batchWrite`、プラグイン設定、複数 Skill、通常のモード切替と復旧、Desktop の新しいタスクでの読み込み。これらの成功は主張しない。

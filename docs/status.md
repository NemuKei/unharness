# Project status

Updated: 2026-09-10.

The [public connection bridge](domain-connection.md) now includes local approval/cancellation and an owned-launcher/MCP handoff without ticket disclosure. Public operations reuse the common source controller and durable ledger; the local screen and MCP can read old public receipts after expiry or restart. [Local approval evidence](evidence/2026-09-10-local-connection-approval.md) records 35 focused backend/contract tests and three built-Chrome browser cases with no skips, including communication failures and mobile layout. Public client/WebMCP and real public-origin/browser qualification remain unfinished; no deployment or personal setting change was made in this pass. The earlier [backend evidence](evidence/2026-09-10-domain-bridge-backend.md) records the wider baseline regression.

[PR #1](https://github.com/NemuKei/unharness/pull/1) is merged into `codex/mac-finish` at `4988059`: the [official-plugin selection and inheritance contract](spec-mode-inheritance.md) is now part of the active Mac Codex goal. The [revised product plan](superpowers/plans/2026-09-09-mac-product-experience.md) starts with read-only provenance/control verification, then paired v2 presets, legacy restoration and shared entry points, before domain/appearance delivery and the final native journey. The merge adopts the contract; the implementation and qualification boundaries are recorded below. Required management controls remain retained before official listing; Normal, historical favorites and existing evidence keep their recorded meaning.

The [first native provenance investigation](evidence/2026-09-10-plugin-provenance-macos.md) distinguishes public listing, installation state and local Skill identities on Codex 0.153.4. Full package-content correspondence and an automatic-only plugin Skill control are still unqualified. Public search/read results did not establish local installation, and the official configuration schema exposes only Skill enablement. Personal configuration stayed unchanged; an old provider-cache version disappeared during the broader observation interval, so whole-profile immutability is not claimed. No production discovery or mode-control code changed.

The [pure inheritance resolver](../src/setup/mode-inheritance.mjs) derives the paired optional sets and rejects unknown provenance, unknown or ambiguous targets, required-control selection, implicit Skill enablement and unsupported invocation changes. Its 35 synthetic cases and nine existing preset/control tests pass. Positive synthetic evidence cannot substitute for a verified native directory/installation binding.

The [v2 stored-preset slice](evidence/2026-09-10-mode-inheritance-storage.md) freezes an internally derived inventory and both mode snapshots, verifies their bytes before forward use, and preserves Normal, current preparation and historical restoration. A versioned workspace manifest fences the actual previous writer; a two-record adoption journal cancels offline after each tested interruption. Registered owned policies support an explicitly reviewed manual-to-automatic change. CLI, authenticated HTTP and stdio MCP produce the same v2 setup and enrollment reviews and preserve operation receipts. V2 enrollment saves only the expanded scope/Normal; setup approval and source preparation remain separate requirements, with offline cancellation and historical restoration checked. The [built workbench](evidence/2026-09-10-mode-inheritance-gui.md) now displays saved inheritance and enforces the separate registration/setup/preparation flow. Its seven focused browser cases pass, with desktop/narrow visual and keyboard checks; related core/API checks pass 144 cases with one platform guard skip. Repeated enrollment and root historical restoration are included. Native plugins remain unknown and unqualified; the updated distribution/native journey is still required. v1 history keeps its earlier interpretation.

The [independent recovery package](plugin-recovery.md) now survives native plugin cache/data removal and exposes only reviewed Normal/retained-settings restoration and journal recovery. [Native update/remove/reinstall evidence](evidence/2026-09-09-plugin-recovery-macos.md) confirms retained records, worker shutdown on cache deletion, in-app restoration without system Node/Codex after retained reconciliation, and the saved `.command` opening the local UI through macOS LaunchServices. Native removal exposed an adjacent-deletion merge case; it is fixed while preserving semantic and comment checks. The fixture ended at Normal with no conflict or pending recovery. Preserve this recovery boundary while implementing the revised mode policy and remaining product experience.

The [Mac arm64 package builder](plugin-package.md) now assembles the official Node runtime, locked production dependencies, UI, MCP and management/setup Skills with a complete content index. The development candidate has [native package evidence](evidence/2026-09-09-plugin-package-macos.md): installed Codex loads the plugin, native MCP opens it before registration, the real in-app browser saves Normal, and native input preparation retains the manager while omitting the manual setup Skill across all modes. Exact Normal and installed-package integrity return unchanged. This is not yet the end-user installer or final release artifact.

The [local plugin connection](plugin-connection.md) now starts MCP before configuration or initial source registration. It binds one explicitly selected Codex context locally, opens the existing reviewed Normal-saving screen, and keeps request receipts through registration and reconnect. Immutable context/root records live outside native plugin data and cache. Corrupt, redirected or replaced bindings fail closed. Its [synthetic CLI/stdio evidence](evidence/2026-09-09-plugin-connection.md) is complemented by the later native package and recovery qualifications; public-origin connection remains in progress.

The [owned workbench launcher](local-workbench.md) now reuses one process for an explicitly selected context, including across first source registration. Registered CLI/MCP opening, authenticated process identity, stop/restart, private receipt recovery boundaries and a real Codex in-app browser prepare/restore/reopen check have [Mac evidence](evidence/2026-09-09-workbench-launch-macos.md). Source data stays outside the launch receipt and plugin cache. This completes the local startup slice; plugin onboarding, public-origin pairing and the remaining product experience still require implementation and qualification.

The active Mac Codex finish now has retained-control guards, persisted release setup and reviewed additive Skill enrollment through CLI, the local workbench and MCP. Setup adoption preserves the original Normal and current preparation. Enrollment creates an expanded Normal while preserving earlier managed content, source files and immutable history, then explicitly requires preparation. Old favorites show how added Skills use saved Normal values; old comparisons retain their own scope. Record-only interruption recovery works without Codex/YAML dependencies, and an actual previous writer refuses expanded snapshots. The full core run passed 750 tests with 26 platform/optional-browser skips; the new browser flows separately cover role review, enrollment, TRUEFORM, historical favorites, narrow layout and lost-response readback. Onboarding, plugin installation, domain pairing, free layered art and the complete native product journey remain in progress.

The [plugin distribution plan](spec-plugin-distribution.md) bundles the related Skills and local MCP, with WebMCP on the public page. The dedicated `deltahelmlab-unharness` Cloudflare Pages project exists and is separate from the existing landing page. No deployment or custom-domain binding is yet qualified.

The built workbench has setup-consultation and fresh-task prompt handoffs. Copying preserves the source files; conflicts suspend the handoff, clipboard failure leaves a selectable prompt, and adopted MCP presets refresh the open GUI. The three focused browser scenarios and desktop/narrow visual review are complete, together with type/CSP/build checks. The existing five AI-to-open-GUI scenarios also pass. These checks do not establish the installer, actual AI consultation or native task handoff.

[Additive enrollment evidence](evidence/2026-09-09-source-enrollment-macos.md) also records the installed 0.153.4 executable preparing all three modes and an older favorite on a new owned Mac profile after expansion from one to two Skills. Exact Normal files/metadata returned, the new Skill body stayed intact, and final conflict/recovery were clear. No personal configuration or model task was used. The final focused tests and the isolated staged build/browser checks pass.

## Established

- Product name: **Unharness**.
- Free to use, with no required paid API, hosted backend, or recurring operator service expense. The user's own AI subscription/usage is explicitly outside this boundary. Default artwork, image composition/import, storage, card export and sharing preparation run locally.
- Value: easily take off and compare harness configurations, then keep the setup that fits the current model, environment, and work.
- Mode labels: **限定解除 — UNSEAL**, **零式 — TRUEFORM**, and Normal loadout.
- Delivery priority updated on 2026-09-09: finish the complete Mac Codex product first. The maintainer deferred Claude Code until they have capacity; keep its existing implementation and regression coverage, but do not require its native qualification for the initial Mac release. Windows follows later, Codex before Claude Code; see [delivery](delivery.md).
- Required experience: save, three modes, truthful state, fresh comparison, favorites, recovery, web and AI entry points, and optional effects.
- Initial release scope centers on user-authored/personally added optional instructions and automatic Skills, with optional hooks secondary. Memory, native continuity and the Unharness management/control Skill remain retained, including in TRUEFORM. The 2026-09-09 [AI-guided setup contract](spec-guided-setup.md) refines the mode choices and new-Skill handling; the [registered user-source contract](spec-user-sources.md) records the currently qualified earlier write boundary. Preserve versions during migration.
- The maintainer clarified that the primary experience uses one selected mode for a task. Record ordinary use and offer an optional later sequential replay; automatic simultaneous multi-mode dispatch is outside initial scope and remains technically unverified.
- Numerical comparison now records persisted Codex root-response usage/time beside attributed retrospective checks, ratings and notes. It keeps partial/missing coverage explicit and does not run an automatic grader. See the [measurement contract](comparison-metrics.md).
- The registered-source workbench has Equipment and Comparison tabs. One to three ordinary-run versions appear as compact historical portraits, an aligned table and a zero-based token chart, and a matched historical configuration can be saved as a favorite without applying it. These are scoped observations; the revised product does not gate artwork creation on them.
- Appearance and performance are independent under the 2026-09-09 revision. Performance is displayed as evidence-backed text/numbers; it does not force the selected image into GOOD/BAD treatment.
- Selected visuals: pixel-art machine hangar, progressive exposure of the inner AI, and a divine luminous reveal.
- Selected rendering stack: PixiJS for the entity, armor and effects; HTML/CSS for controls and readable state; reviewed PNG sprites plus versioned JSON recipes/pixel data. The maintainer accepts additional dependencies. See [the rendering decision](design.md#selected-rendering-stack).
- Prepared/random default discovery remains available. Original creation is voluntary at any time through the user's own AI and a bundled authoring Skill, with local import and reusable collection versions. [The new appearance contract](personalization.md) removes comparison unlocks, three-candidate/final-choice limits and forced BAD art.
- Original templates define three logical parts: AI entity, restraints and background. Users can replace one part or a complete set; compatible prepared parts, anchors and mode poses support composition. The old appearance core and unfinished GUI/API work still need migration and rendered verification.
- [Build-card export/X preparation](build-cards.md) remains planned with local image saving as a fallback and explicit user posting. Reusable artwork packs and public discovery are later design work after local creation/save/reuse; they do not add a public gallery requirement to Mac completion.
- The accepted primary entry is a user-owned domain inside the AI app's browser, opened through the Unharness management Skill. [The domain-entry contract](spec-domain-entry.md) includes local pairing, source privacy and offline recovery; the maintainer authorized a subdomain under the existing SideBiz site. The selected destination is `https://unharness.deltahelmlab.com`, with a small DeltaHelm Lab link to `https://deltahelmlab.com/`. Cloudflare Pages hosting remains separate from the existing landing-page project; DNS binding and deployment are not yet verified.
- The [Mac product plan](superpowers/plans/2026-09-09-mac-product-experience.md) includes the five recommended UX items: a synthetic demo, clear connection identity, fresh-task guidance, offline recovery and retained Unharness controls, plus a small author/site footer. These are planned changes, not newly verified runtime behavior.
- Initial README drafts, specification, architecture boundaries, compatibility criteria, and handoff plan are present.

## Current implementation

Node.js 24+ diagnostics use ES modules and the standard library. `inspect` queries a separate app-server; `probe-controls` tests six CLI-rendered fixture conditions. `inspect-desktop` projects one selected local desktop recording (`--current` or `--session`), and `desktop-fixture` prepares/restores a persistent synthetic project for fresh-task checks. The shared fixture service saves immutable favorites, plans/restores them with checkpoints and associates recordings with application receipts; its [local GUI](gui.md) calls those same operations. The separate `sources` service and explicitly launched [registered-source workbench](user-source-gui.md) now prepare selected optional global Codex instructions and Skills and store private ordinary-run reviews/assessment versions, with frozen Normal/favorites, guarded plans, supported Mac metadata and Node-only recovery. UNSEAL compilation uses the locked YAML dependency; the existing diagnostic commands and frozen recovery remain independent of browser dependencies. Sequential replay and a registered local MCP transport are implemented. The built GUI now receives MCP changes with bounded read-only updates. The registered Mac Codex core now has actual desktop AI qualification as well. Full runtime coverage, an installer and the remaining Mac product features are still open; this is not a completed Mac release. See [inventory](codex-probe.md), [source controls](source-controls.md), [desktop observation](desktop-observation.md), [ordinary-run records](spec-comparison-records.md), and [registered loadouts](loadouts.md).

The implemented probe completed real Mac and native Windows read-only runs against Codex 0.153.4. On Windows 11 x64 with desktop package 26.901.6511.0 and bundled Node 24.19.0, the selected source-file content/existence checks were unchanged and `inspect-desktop --current` recognized one selected user-created task. This is persisted-record projection, not a desktop attachment or mode application. See [the Windows baseline](evidence/2026-09-07-windows-baseline.md).

The GUI uses React/TypeScript and Vite with PixiJS 8.20.1. Its current animation draws 49 coherent cels from original foreground parts: rigid armor folds about its seams, supports retreat and the core rises against fixed architecture. Cutouts are baked locally once, and reverse/interrupted travel uses the same pose table. Foreground idle motion remains continuous. Equipment and Comparison share one accepted source controller and operation lock. Comparison renders static portrait crops, semantic tables and CSS bars without another renderer or chart dependency. The CLI/core does not load browser dependencies. It distinguishes confirmed failure, uncertain private publication and failed auxiliary reads; a comparison error does not remove the Equipment tab or recovery controls. Original-form creation and collection runtime remain future work.

The integrated Windows browser smoke at `8993a00` exercised select, apply, save and checkpoint restore in the earlier built loopback GUI. Its UUID form then observed one qualified fresh baseline task as `not-matched-record`, not a successful baseline association. Browser restart, effects-off, narrow layout, the current animation revision and a matching Windows fresh-task observation remain untested.

The implemented source-control command also completed a real Mac run against 0.153.4: manual-only catalog exclusion, SKILL.md-based disablement, fixed-only AGENTS override, restoration, and fixture cleanup passed. Directory-based disablement did not exclude the fixture. Three selected personal configuration/instruction locations were unchanged. The synthetic suite passed 52/52 tests. See [the source-control evidence](evidence/2026-09-06-source-controls-macos.md); these results still do not verify a desktop mode switch or explicit manual invocation.

The desktop diagnostic runtime at `7889864` was reviewed and the full suite passed 82/82 tests. New checks cover privacy projection, wrong/stale/forked task candidates, interrupted fixture creation/change/recovery/cleanup, duplicate and concurrent callers, unknown edits, staged publication and link boundaries. The real Mac development task's recording exposed host Skill catalog and memory guidance in initial input, recognized source-state fields, and usage-field availability. These are recording observations; no token totals or complete task/child usage were collected. See [Mac evidence](evidence/2026-09-06-desktop-observation-macos.md).

The [fresh Mac sequence](evidence/2026-09-06-desktop-fixture-macos.md) now has nine plain-task observations: baseline, manual-only omission, fixed-only AGENTS, and restoration with refresh. Literal `$skill` invocation read the synthetic Skill and delivered its body; desktop picker selection remains untested. Two restored tasks retained a stale catalog. A later user-created task and subsequent tool-created task saw it again; a repeated same-route trial restored visibility after an owned Skill mtime notification. This single successful notification trial is not a general reload guarantee.

The follow-up diagnostic at `ae487b9` handles custom-tool text-block outputs, records task-creation routes, excludes known agent forks, and provides `desktop-fixture refresh` with interrupted-operation recovery. The full suite passes 88 tests. Personal settings/permissions were not switched; three selected personal configuration/instruction locations were unchanged across the sequence. No app restart was requested. The fixture is restored to baseline and retained for its saved desktop project; full product mode switching remains unimplemented.


The registered-loadout core at `29c13e2` now completes a synthetic native Mac CLI loop: register → save baseline → save a changed version → restore an exact favorite → restore the pre-change checkpoint. The same family/name/settings keep the same favorite version across preparation-only refreshes. Independent edits and stale plans block restoration. The full suite passes 126 tests, including corrupt-record handling, interrupted saves/restores and version-bound observation checks. The new [loadout evidence](evidence/2026-09-06-loadouts-macos.md) is filesystem/service evidence; no new desktop task was associated with a favorite in that smoke run.

The [saved-version desktop loop](evidence/2026-09-07-saved-loadout-desktop-macos.md) now connects the service to actual Mac tasks. A saved manual-only version and the restored baseline each produced a completed fresh task and a `matched-record` observation tied to the exact favorite/application. An older task from the same project was `unqualified-record`. A refresh was followed by a replacement application receipt before the baseline task; the original baseline favorite ID was reused. Recorded model/execution conditions matched, three selected personal locations were unchanged, and the retained fixture is at baseline. These are fixture-scoped recording observations, not full runtime/mode verification.

The [native Windows baseline](evidence/2026-09-07-windows-baseline.md), for runtime `13c5902` and test commit `af91a9a`, completed the six-case source-control matrix, native fixture/recovery and loadout save/checkpoint/restore smoke. The integrated GUI revision `8993a00` then passed 148 tests with one existing POSIX-FIFO skip, plus type checks and a production build; its in-app-browser smoke completed selection, application, save and checkpoint restoration. The [first Windows fresh task](evidence/2026-09-07-windows-fresh-task.md) was qualified by route, cwd and timing, but its baseline result was `not-matched-record`, with the expected Skill catalog marker absent. Manual-only and `SKILL.md` file exclusion worked in the CLI fixture; directory exclusion remained ineffective, matching the Mac observation. Windows baseline match, successful saved-version association, later cases, ACL isolation, named-pipe behavior, real personal-source control and product modes remain unresolved.

The [local GUI check](evidence/2026-09-07-local-gui-macos.md) records 149 passing tests, type/CSP checks, a production build, and real in-app-browser select/apply/save/checkpoint-restore behavior. The same store was reopened after restart. Desktop, laptop and narrow viewports were inspected. The Windows baseline adds a bounded normal-viewport GUI smoke, and [the first Windows fresh task](evidence/2026-09-07-windows-fresh-task.md) exercises the UUID observation path without a baseline match. Neither result establishes full runtime or mode state.

The [motion and control-clarity follow-up](evidence/2026-09-07-gui-motion-macos.md) records 156 passing tests, type/CSP checks and a production build. Browser checks cover continuous layered motion, forward/reverse and interrupted transitions, effects/reduced motion, responsive development details, and primary undo after a second client changes the fixture. The same fixture is back at baseline; full runtime/mode verification remains false.

The maintainer then reselected the original three-state painting. That v3 correction restored the source artwork but was subsequently rejected for unnatural motion. The [current animation QA](../design-qa.md) records the v4 mechanical-cel rewrite, including the [49-cel sheet](assets/07-mechanical-cels-v4.png) and [recorded motion](assets/08-mechanical-motion-v4.webm). All 160 tests and the type/CSP/build checks pass; rigid edge lengths/thickness, part order and source bounds are covered. Mac browser checks exercise the actual seam rotation, reverse retargeting, effects/reduced motion and narrow layout. The retained fixture preparation did not change during this display-only pass.

After accepting that motion, the maintainer requested one visual refinement: stronger light around the fully released AI entity. A source-shaped white-blue radiance now ramps in at final release and pulses slowly, with its intensity adjusted to preserve branch detail. Effects off removes the added light. The cel motion and configuration operations remain the same.

The maintainer accepted the combined motion and radiance at `a0ce80c` as the visual baseline on 2026-09-07. The next milestone is the desktop setting/observation/recovery loop; visual acceptance does not establish configuration or runtime verification.

The [read-only source inventory](source-inventory.md) now connects a real, explicitly selected project to both `inspect-sources` and the GUI. It combines existing RPC counts/layers with bounded standard AGENTS candidate metadata, keeps ownership/role/loading unknowns visible, and never registers or changes personal sources. The browser presents a changed startup context before collection, including after a failed metadata request. Candidate instruction/Skill/hook categories are visible first; retained information is collapsed. [Mac evidence](evidence/2026-09-08-source-inventory-macos.md) records 176 passing tests, type/CSP/build checks, actual read/reread/reload and same-port restart cases, narrow layout and unchanged selected personal files.

The [registered-source pass](evidence/2026-09-08-user-source-modes-macos.md) adds explicit optional-role registration, fixed-guide/manual-Skill UNSEAL, selected-source TRUEFORM, exact Normal restoration and versioned favorites/checkpoints. The dedicated workbench reopens registrations through a read-only locator and binds every action to the accepted launch/context; fixture routes remain separate. Native owned-profile checks cover metadata, interruption, independent edits and offline recovery. Built-browser checks exercise registration, all three modes, favorite/undo, pending recovery, same-port context changes and narrow layout. The final `ec5f3cc` runtime passed 264 tests with one platform skip and a fresh native Mac check; ownership admission now distinguishes portable planning from qualified publication. At that stage the maintainer's real profile had only been discovered; the subsequent real-source sequence below registered and prepared the selected sources. Native Windows source writes remain gated, and both full runtime/mode verification flags stay false.

The [native configuration follow-up](evidence/2026-09-08-native-config-comments-macos.md) uses path-specific Skill edits to preserve unrelated array comments while retaining the guarded duplicate-entry operation. Native regressions, owned-profile recovery, 265 full-suite tests with one skip, focused final tests and the build passed. A temporary copy of the real configuration compiled without changing its original.

The [real-source Mac sequence](evidence/2026-09-08-real-source-desktop-macos.md) then established the maintainer's chosen Normal from the original global instructions and restored existing Skills, registered one optional global source and one Skill, and exercised Normal → UNSEAL → TRUEFORM → saved Normal through the workbench. Four fresh local tasks recorded the expected selected-source patterns with matching recorded model/execution conditions. Literal manual Skill reading worked in UNSEAL; an old-task control was unqualified. The final snapshot and fresh-task sources returned to Normal, using the original favorite version. These are private operator-associated observations. The subsequent [registered-source observation implementation](spec-user-source-observations.md) adds persisted preparation boundaries and dated task associations to the shared service, CLI and workbench. It uses only the selected task's canonical initial instruction/catalog fields and suppresses stale or conflicting current matches. The old pilot receipts are not retrofitted; full runtime/mode flags remain false. [Final observation evidence](evidence/2026-09-08-user-source-observations-macos.md) records the integrated tests, native owned-profile loop, browser statuses/reconnection/legacy checks, and exact Normal/favorite/observation snapshot identity. A separate real-profile recheck detected a retained `service_tier` edit as a conflict and preserved it.

The [retained-settings implementation](spec-retained-settings.md) now lets the source workbench review a retained-only Codex configuration conflict and explicitly record the exact safe plan as a new active Normal version without changing managed files. Its HTTP routes keep accepted context, request-identity deduplication and stale-plan checks; the React state clears cached plans when scope, revision, active Normal or launch context changes. Older favorite/checkpoint plans show that current common settings will be retained, while saved records remain immutable. [Native and built-browser qualification](evidence/2026-09-08-retained-settings-macos.md) covers all three starting modes, immutable old favorites/checkpoints, an actual older-writer fence, exact source preservation, narrow views, second-client changes and interruption recovery. Recovery cancels private recording state and leaves managed files unchanged. The final review's native non-finite-value and stale-plan response cases were fixed; 393 tests passed with one platform skip, and the affected native/browser checks passed. The existing real registration then accepted its exact reviewed retained-only plan with no managed-file changes. That adoption ended at Normal revision 4 with the original Normal and three favorite records preserved, no conflict and no pending recovery. A later independent retained-setting conflict was recorded during the ordinary-comparison pass and was not accepted there. Full desktop verification flags remain false.

The [ordinary-comparison qualification](evidence/2026-09-08-comparison-records-macos.md) corroborates four existing native recordings and verifies private history, immutable assessments, strict HTTP input, recorded-value charts, unknown/overlapping evidence, historical favorites and restoration. Built-browser checks cover repeated UUID handoffs, delayed replies, transport failure, changed scope, effects off, narrow layout and renderer/artwork failure. The final suite passed 458 tests with one existing platform skip, including the optional built-browser regression. The owned profile ended at Normal revision 12 with exact managed-file restoration; no new model task or personal configuration write was needed. These are observational records, not a performance verdict or a full desktop-mode qualification.

The [pre-use starting-conditions implementation](spec-starting-conditions.md) now freezes explicit requests, criteria/budgets and original working-file bytes through the shared registered-source CLI and Comparison form. Input chunks and manifests are isolated from ordinary history, and saved starts remain immutable. [Native Mac and built-browser evidence](evidence/2026-09-08-starting-conditions-macos.md) covers original/binary/missing inputs, independent edits, input integrity, duplicate and uncertain saves, restart/scope changes, effects off and narrow screens. Source configuration/state stayed unchanged in the owned native profile and no model task was started. This is the input foundation for replay, not a task association or performance verdict.

## Next useful work

Follow the [six-step inheritance plan](superpowers/plans/2026-09-10-official-plugin-mode-inheritance.md). First establish the actual selected Mac/Codex version's official-directory/installed-content correspondence and supported automatic-use controls without modifying personal sources. Implement the derived sets and versioned storage through the shared core, then qualify both UI and AI. Public pairing and free art remain required work; if the provenance/control investigation leaves a concrete unresolved capability, continue independent work without claiming the new modes qualified.

The [replay preparation service](evidence/2026-09-09-replay-preparation-macos.md) builds distinct owned work locations from frozen inputs, maps approved repo Skill variants and checks retained native conditions. The subsequent [task/outcome collection](evidence/2026-09-09-replay-results-macos.md) associates supported first-request/turn/source evidence with a validated attempt, freezes separate binary outcomes and records attributed assessments against the original criteria and budgets. Native user/delegation request shapes were corroborated from existing synthetic tasks. Direct review covered concurrent recording appends, reverted source changes, missing-turn usage, corrupt outcomes and lost publication.

The [CLI/GUI and native replay qualification](evidence/2026-09-09-replay-gui-macos.md) now connects all twelve replay operations to Comparison. It covers source/file rechecks before copying/opening, strict request/response identities, lost responses, stale UUIDs, changed contexts, historical favorites and overlapping recorded task timelines. Four new Mac desktop tasks matched the exact frozen request, selected sources and recorded retained conditions for Normal, UNSEAL, TRUEFORM and restored Normal. Each used a separate owned worktree from one saved start and saved an attributed result through the workbench. The exact Normal favorite restored the managed bytes/metadata; the real profile ended at Normal with no conflict, recovery or active attempt. The complete suite passed 573 of 574 tests with the existing platform skip, all optional browser cases ran, type/CSP/build checks passed, and the restarted built GUI retained history and the three-mode comparison at desktop and narrow widths. Results remain neutral; this is scoped native recording evidence, not complete runtime coverage or a performance verdict.

The active goal is Mac completion under the [delivery order](delivery.md). The selected real-source/fresh-task sequence, source-observation workbench, retained-setting reconciliation, ordinary comparison records, pre-use input capture and sequential replay through CLI/GUI are implemented. The registered [AI/MCP transport](ai-commands.md) now has official-client and separate native app-server evidence. The [open-screen update checks](evidence/2026-09-09-ai-gui-updates-macos.md) also pass. The [actual native AI sequence](evidence/2026-09-09-ai-desktop-macos.md) now completes the registered-source Mac Codex Phase 1 audit: it preserves the retained local connection, records four matching fresh tasks, compares independent results and restores exact Normal bytes/metadata. The 18 focused MCP recovery/server/request tests also pass, including killed-process source recovery and independent edits. The source/test/evidence baseline `1cac327` is integrated into private main. The [Claude Code Mac handoff](handoff-claude-macos.md) produced the retained adapter on the isolated `codex/claude-macos` worktree (desktop 1.49585.0). Its remaining native work is deferred and does not block the active Mac Codex product finish. The fixed minimal guide is defined and versioned; configuration readback remains separate from task-loaded evidence. The [feasibility boundaries](feasibility.md) still apply.

Windows work is deferred until the Mac product milestone. Preserve its current evidence and fixture receipts: the first fresh baseline task did not match; the owner subsequently refreshed the unchanged Skill and reapplied the same baseline favorite. When Windows work resumes, repeat that user-created route with a new task and inspect its recording before changing cases. Continue the later modes only after a matching baseline. Windows source ownership, filesystem qualification and desktop control remain unresolved, with real-source writes still gated. Do not infer a cache cause from the first mismatch.

The Windows baseline retains its desktop/runtime versions and native results. The scoped Mac Codex Phase 1 criteria are now met.

Phase 2 is implemented on `codex/claude-macos`. An [application seam](architecture.md) now resolves one adapter per registration, with the Codex path preserved exactly — the same 616 tests pass as at the baseline. The [Claude Code Mac adapter](claude-macos.md) covers discovery, three modes, exact Normal restoration, favorites, checkpoints, interruption recovery, retained-settings reconciliation, task observation and ordinary-run measurement, verified against owned fixtures, the built loopback interface, an MCP stdio connection, four Playwright cases and one real 1,066-record Claude Code 2.1.260 recording. Two review findings were fixed: a retained number outside the double range was being rewritten, and Skill state was read from the user settings layer alone when Claude Code applies managed over project-local over project over user.

The single unresolved Phase 2 step is operator-assisted: a fresh native Code task per mode against the owned fixture, plus the native AI connection. Its exact fixture, launch mechanism, request, expected observation and restoration are in [native qualification](claude-native-qualification.md). Until it runs, `runtimeStateVerified` and `modeSwitchingVerified` stay false for Claude and `sourceCoverage` stays unknown. Sequential replay is refused for Claude with a recorded reason; registering the maintainer's real `~/.claude` is currently impossible and needs an explicit personal decision, not an inference. Codex reviews that qualification when Claude work resumes. The initial Mac Codex product finish proceeds independently; Windows Codex and Claude Code follow later.

## Before public release

The maintainer selected the MIT License on 2026-09-09 and clarified the public goal: let people enjoy a usable free product and make the implementation and care visible as a body of work. The root license and package metadata now reflect that choice. Configure real reporting/contribution channels, publish tested installation and recovery instructions, add the product favicon, and replace draft availability statements with the verified compatibility matrix. The playable site, installable plugin, source/verification records and author link belong to the same release. These are release preparation items, not claims of current support.
